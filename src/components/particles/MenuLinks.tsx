import { Box, styled } from "@mui/material";

export const MenuLinks = styled(Box)(() => ({
    fontSize: '15px',
    display: 'flex',
    alignItems: 'center',
    gap: '15px',
    padding: '7px 0',
    marginTop: '-5px'
}))
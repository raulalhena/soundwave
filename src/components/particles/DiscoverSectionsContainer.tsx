import { Container, styled } from "@mui/material";

export const DiscoverSectionsContainer = styled(Container)(() => ({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'start',
    gap: '15px',
    border: '1px solid red',
    padding: '0'
  }))
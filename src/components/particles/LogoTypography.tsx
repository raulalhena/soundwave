import { styled, Typography } from "@mui/material";

export const LogoTypography = styled(Typography)(() => ({
    display: 'inline-flex',
    gap: '10px',
    fontSize:'20px',
    alignItems: 'center', 
    width: '100%',
    color: '#fff'
}))
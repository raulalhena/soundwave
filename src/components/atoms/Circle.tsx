import { Box, styled } from '@mui/material'

export const Circle = styled(Box)(() => ({
    display: 'flex',
    width: '350px',
    height: '350px',
    opacity: '0.2',
    backgroundColor: '',
    borderRadius: '50%'
}))
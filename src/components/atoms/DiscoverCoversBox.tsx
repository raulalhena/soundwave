import { Box, styled } from "@mui/material";

export const DiscoverCoversBox = styled(Box)(() => ({
    width: '100%', 
    border: '1px solid purple', 
    display: 'flex', 
    justifyContent: 'end', 
    padding: '0'
}))
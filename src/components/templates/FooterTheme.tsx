import { createTheme } from "@mui/material";

export const footerTheme = createTheme({
    palette: {
        primary: {
            main: '#202027',
        },
        text: {
            primary: '#000'
        },
        background: {
            primary: '#202027'
        }
    }
})